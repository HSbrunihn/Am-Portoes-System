# backend/

Cole aqui o conteúdo do projeto Spring Boot (pom.xml, mvnw, src/, etc.) e apague este arquivo.

Depois:
```bash
git add backend/
git commit -m "feat(backend): estrutura inicial do projeto Spring Boot"
git push
```
