import { Component,inject,OnInit,signal } from '@angular/core'; import { forkJoin } from 'rxjs'; import { ApiService } from '../../services/api.service'; import { Orcamento,Produto,Tarefa,Usuario } from '../../models/api.model';
@Component({selector:'app-dashboard',templateUrl:'./dashboard.html',styleUrl:'./dashboard.scss'}) export class Dashboard implements OnInit{private readonly api=inject(ApiService);readonly loading=signal(true);readonly error=signal('');readonly totals=signal({tarefas:0,produtos:0,orcamentos:0,usuarios:0,pendentes:0});ngOnInit():void{forkJoin({tarefas:this.api.list<Tarefa>('tarefas'),produtos:this.api.list<Produto>('produtos'),orcamentos:this.api.list<Orcamento>('orcamentos'),usuarios:this.api.list<Usuario>('usuarios')}).subscribe({next:r=>{this.totals.set({tarefas:r.tarefas.length,produtos:r.produtos.length,orcamentos:r.orcamentos.length,usuarios:r.usuarios.length,pendentes:r.tarefas.filter(t=>t.status==='PENDENTE').length});this.loading.set(false)},error:()=>{this.error.set('Não foi possível conectar à API. Inicie o backend e tente novamente.');this.loading.set(false)}})}}
