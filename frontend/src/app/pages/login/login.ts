import { HttpErrorResponse } from '@angular/common/http'; import { Component,inject,signal } from '@angular/core'; import { FormControl,FormGroup,ReactiveFormsModule,Validators } from '@angular/forms'; import { Router } from '@angular/router'; import { AuthService } from '../../services/auth.service';
@Component({selector:'app-login',imports:[ReactiveFormsModule],template:`<main><section class="hero"><div><b>AM</b><h1>Gestão que abre caminhos.</h1><p>Organize tarefas, produtos e orçamentos em um só lugar.</p></div></section><section class="area"><form [formGroup]="form" (ngSubmit)="submit()"><span>ACESSO AO SISTEMA</span><h2>Bem-vindo</h2><p>Informe suas credenciais para continuar.</p>@if(error()){<div class="alert">{{error()}}</div>}<label>E-mail<input type="email" formControlName="email" autocomplete="username" placeholder="voce@empresa.com.br">@if(form.controls.email.touched&&form.controls.email.invalid){<small>Informe um e-mail válido.</small>}</label><label>Senha<input type="password" formControlName="senha" autocomplete="current-password" placeholder="Sua senha">@if(form.controls.senha.touched&&form.controls.senha.invalid){<small>A senha é obrigatória.</small>}</label><button [disabled]="loading()">{{loading()?'Entrando…':'Entrar'}}</button><footer>AM Portões • Sistema de gestão</footer></form></section></main>`,styles:[`main{min-height:100vh;display:grid;grid-template-columns:1.05fr 1fr}.hero{background:linear-gradient(145deg,#0e2941,#1a4b6d);color:#fff;display:flex;align-items:end;padding:10%;position:relative}.hero:after{content:'';position:absolute;inset:0;background:radial-gradient(circle at 70% 20%,#f68b1f33,transparent 35%)}.hero div{position:relative;z-index:1}.hero b{display:grid;place-items:center;width:64px;height:64px;background:#f68b1f;border-radius:17px;font-size:22px}.hero h1{font-size:46px;max-width:500px;margin:28px 0 12px}.hero p{color:#c6d6e1}.area{display:grid;place-items:center;background:#f7f9fa;padding:25px}form{width:min(410px,100%);background:#fff;padding:38px;border-radius:16px;box-shadow:0 20px 60px #16364f18}form>span{font-size:11px;color:#ef7d17;font-weight:800;letter-spacing:1.4px}h2{font-size:32px;color:#183b56;margin:8px 0}form>p{color:#718391;margin-bottom:28px}label{display:block;margin:17px 0;color:#344f63;font-weight:700;font-size:13px}input{display:block;width:100%;box-sizing:border-box;margin-top:7px;padding:12px;border:1px solid #cad5dc;border-radius:8px}small{color:#b42318}button{width:100%;padding:13px;margin-top:8px;border:0;border-radius:8px;background:#ef7d17;color:#fff;font-weight:700}.alert{padding:12px;background:#fff0ef;color:#a22c25;border-radius:8px}footer{text-align:center;color:#91a0ab;font-size:11px;margin-top:25px}@media(max-width:760px){main{grid-template-columns:1fr}.hero{display:none}.area{min-height:100vh}form{padding:28px}}`]})
export class LoginPage{private readonly auth=inject(AuthService);private readonly router=inject(Router);readonly loading=signal(false);readonly error=signal('');readonly form=new FormGroup({email:new FormControl('',{nonNullable:true,validators:[Validators.required,Validators.email]}),senha:new FormControl('',{nonNullable:true,validators:[Validators.required]})});submit():void{if(this.form.invalid){this.form.markAllAsTouched();return}this.loading.set(true);this.error.set('');this.auth.login(this.form.controls.email.value,this.form.controls.senha.value).subscribe({next:()=>void this.router.navigateByUrl('/dashboard'),error:(e:HttpErrorResponse)=>{this.loading.set(false);this.error.set(e.status===401?'E-mail ou senha inválidos.':'Não foi possível conectar ao servidor.')}})}}
